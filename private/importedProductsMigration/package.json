{
  "name": "imported-products-migration",
  "version": "1.0.0",
  "description": "Update appSearch products with the the stores products from the products-instances index",
  "main": "index.js",
  "scripts": {
    "im": "export $(cat .env) && node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ElGreetly",
  "license": "ISC",
  "dependencies": {
    "@elastic/app-search-node": "^7.7.0",
    "@elastic/elasticsearch": "^7.7.1"
  }
}
